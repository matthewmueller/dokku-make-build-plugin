# dokku-make-build-plugin

Runs `make build` before you deploy

## Installation

Log into your server and run:

    git clone https://github.com/matthewmueller/dokku-make-build-plugin /var/lib/dokku/plugins/dokku-make-build
